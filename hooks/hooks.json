{
  "description": "skills-constitution — 宪法强制拦截钩子（SessionStart 注入 + UserPromptSubmit 校验）v2.22.0；CodeBuddy/Claude Code 插件机制版，WorkBuddy 用 scripts/constitution-gate.py（见 SKILL.md 安装章节）",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CODEBUDDY_PLUGIN_ROOT}/hooks/session-start.sh\"",
            "timeout": 30,
            "description": "宪法 SessionStart 钩子 v2.13.2：注入记忆+技能树完整注入块到 stdout（前台执行保留输出）"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CODEBUDDY_PLUGIN_ROOT}/hooks/user-prompt-submit.sh\" \"$USER_PROMPT\"",
            "timeout": 10,
            "description": "宪法 UserPromptSubmit 钩子 v2.22.0：任务分类 + 注入校验（保留退出码，拦截真实生效）"
          }
        ]
      }
    ]
  }
}
